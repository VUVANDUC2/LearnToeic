<!-- ====== PART 1: PHOTO (IMAGE_CHOICE) ====== -->
<th:block th:fragment="part1(pv, takeId)" th:remove="tag">
  <section >
    <div class="vstack gap-3 questions-scroll">

      <!-- Audio player dùng chung cho toàn Part -->
      <div th:if="${pv.part == 1}" class="mb-3">
      <audio controls preload="none" class="w-100">
          <source src="/assets/tests/p001/test_1_1.mp3" type="audio/mpeg"/>
          Trình duyệt của bạn không hỗ trợ phát audio.
      </audio>
      </div>


      <div th:each="q, it : ${pv.questions}"
          class="card question-card p-3 p-lg-4"
          th:attr="data-type=${q.questionType}"
          th:id="${'q' + (it.index + 1)}">

          <!-- Header: Số câu thật từ DB -->
          <div class="d-flex align-items-center justify-content-between mb-2">
              <span class="badge text-bg-secondary">
              Câu [[${q.questionNumber}]]
              </span>
          </div>

        <!-- ✅ Nếu có ảnh thì hiển thị -->
          <!-- <img
            th:src="@{|/assets/tests/p001/test_1_${q.questionNumber}.png|}"
            th:alt="${'Hình cho câu ' + q.questionNumber}"
            class="w-100 rounded mb-3"
            loading="lazy"
            onerror="this.onerror=null; this.src='/assets/placeholder/no-image.png';"> -->
        <!-- Chỉ render ảnh khi là Part 1 hoặc khi có field hasImage -->
        <img th:if="${pv.part == 1 or (q.hasImage ?: false)}"
            th:src="@{|/assets/tests/p001/test_1_${q.questionNumber}.png|}"
            class="w-100 rounded mb-3" loading="lazy">


        <!-- Các đáp án -->
        <div class="answer-form" th:attr="data-qn=${q.questionNumber}">
          <!-- A -->
          <label class="option d-block mb-2"
                th:classappend="${selectedMap != null and selectedMap[q.questionNumber] == 'A'} ? ' active'">
            <input type="radio"
                  th:name="${'q' + q.questionNumber}"
                  value="A"
                  th:checked="${selectedMap != null and selectedMap[q.questionNumber] == 'A'}"/>
            <strong>A</strong>. <span th:text="${q.optionA}">Option A</span>
          </label>

          <!-- B -->
          <label class="option d-block mb-2"
                th:classappend="${selectedMap != null and selectedMap[q.questionNumber] == 'B'} ? ' active'">
            <input type="radio"
                  th:name="${'q' + q.questionNumber}"
                  value="B"
                  th:checked="${selectedMap != null and selectedMap[q.questionNumber] == 'B'}"/>
            <strong>B</strong>. <span th:text="${q.optionB}">Option B</span>
          </label>

          <!-- C -->
          <label class="option d-block mb-2"
                th:classappend="${selectedMap != null and selectedMap[q.questionNumber] == 'C'} ? ' active'">
            <input type="radio"
                  th:name="${'q' + q.questionNumber}"
                  value="C"
                  th:checked="${selectedMap != null and selectedMap[q.questionNumber] == 'C'}"/>
            <strong>C</strong>. <span th:text="${q.optionC}">Option C</span>
          </label>

          <!-- D -->
          <label class="option d-block mb-2"
                th:classappend="${selectedMap != null and selectedMap[q.questionNumber] == 'D'} ? ' active'">
            <input type="radio"
                  th:name="${'q' + q.questionNumber}"
                  value="D"
                  th:checked="${selectedMap != null and selectedMap[q.questionNumber] == 'D'}"/>
            <strong>D</strong>. <span th:text="${q.optionD}">Option D</span>
          </label>

          <!-- Nút xóa chọn -->
          <button type="button"
                  class="btn btn-sm btn-outline-secondary btn-clear"
                  th:attr="data-qn=${q.questionNumber}">
            Xóa chọn
          </button>
        </div>
      </div>
    </div>
  </section>
</th:block>


<!-- ====== PART 2: QUESTION-RESPONSE (AUDIO_ONLY) ====== -->
<th:block th:fragment="part2(pv, takeId)" th:remove="tag">
  <section >
    <div th:each="q, it : ${pv.questions}"
          class="card question-card p-3 p-lg-4"
          th:attr="data-type=${q.questionType}"
          th:id="${'q' + (it.index + 1)}">

          <!-- Header: Số câu thật từ DB -->
          <div class="d-flex align-items-center justify-content-between mb-2">
              <span class="badge text-bg-secondary">
              Câu [[${q.questionNumber}]]
              </span>
          </div>
          <!-- Audio player dùng chung cho toàn Part -->
      <div class="mb-3">
      <audio controls preload="none" class="w-100">
          <source src="/assets/tests/p001/test_1_1.mp3" type="audio/mpeg"/>
          Trình duyệt của bạn không hỗ trợ phát audio.
      </audio>
      </div>
        <!-- Các đáp án -->
        <div class="answer-form" th:attr="data-qn=${q.questionNumber}">
          <!-- A -->
          <label class="option d-block mb-2"
                th:classappend="${selectedMap != null and selectedMap[q.questionNumber] == 'A'} ? ' active'">
            <input type="radio"
                  th:name="${'q' + q.questionNumber}"
                  value="A"
                  th:checked="${selectedMap != null and selectedMap[q.questionNumber] == 'A'}"/>
            <strong>A</strong>. <span th:text="${q.optionA}">Option A</span>
          </label>

          <!-- B -->
          <label class="option d-block mb-2"
                th:classappend="${selectedMap != null and selectedMap[q.questionNumber] == 'B'} ? ' active'">
            <input type="radio"
                  th:name="${'q' + q.questionNumber}"
                  value="B"
                  th:checked="${selectedMap != null and selectedMap[q.questionNumber] == 'B'}"/>
            <strong>B</strong>. <span th:text="${q.optionB}">Option B</span>
          </label>

          <!-- C -->
          <label class="option d-block mb-2"
                th:classappend="${selectedMap != null and selectedMap[q.questionNumber] == 'C'} ? ' active'">
            <input type="radio"
                  th:name="${'q' + q.questionNumber}"
                  value="C"
                  th:checked="${selectedMap != null and selectedMap[q.questionNumber] == 'C'}"/>
            <strong>C</strong>. <span th:text="${q.optionC}">Option C</span>
          </label>

          <!-- D -->
          <label class="option d-block mb-2"
                th:classappend="${selectedMap != null and selectedMap[q.questionNumber] == 'D'} ? ' active'">
            <input type="radio"
                  th:name="${'q' + q.questionNumber}"
                  value="D"
                  th:checked="${selectedMap != null and selectedMap[q.questionNumber] == 'D'}"/>
            <strong>D</strong>. <span th:text="${q.optionD}">Option D</span>
          </label>

          <!-- Nút xóa chọn -->
          <button type="button"
                  class="btn btn-sm btn-outline-secondary btn-clear"
                  th:attr="data-qn=${q.questionNumber}">
            Xóa chọn
          </button>
        </div>
      </div>
  </section>
</th:block>

<!-- ====== PART 3: SHORT CONVERSATIONS ====== -->
<th:block th:fragment="part3(pv, takeId)" th:remove="tag">
  <div class="alert alert-info">Part 3 chưa triển khai</div>
</th:block>

<!-- ====== PART 4…7: Tùy cấu trúc của bạn (đoạn văn, điền chỗ trống, reading…) ====== -->
<th:block th:fragment="part4(pv, takeId)" th:remove="tag">
  <div class="alert alert-info">Part 4 chưa triển khai</div>
</th:block>

<th:block th:fragment="part5(pv, takeId)" th:remove="tag">
  <div class="alert alert-info">Part 5 chưa triển khai</div>
</th:block>
<th:block th:fragment="part6(pv, takeId)" th:remove="tag">
  <div class="alert alert-info">Part 6 chưa triển khai</div>
</th:block>